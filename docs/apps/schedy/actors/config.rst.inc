These are all settings that can be configured for an actor, either on a per-actor
basis or for multiple actors at once in a template under ``actor_templates:``. The
default values are shown here. If you are happy with the defaults for particular
settings, simply omit these settings or leave them commented out.

.. literalinclude:: config.yaml
   :language: yaml
